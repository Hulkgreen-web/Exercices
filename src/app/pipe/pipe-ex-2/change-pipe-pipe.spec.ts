import { ChangePipePipe } from './change-pipe-pipe';

describe('ChangePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangePipePipe();
    expect(pipe).toBeTruthy();
  });
});
